import React from 'react';

const LandingPage: React.FC = () => {
  // This component is no longer used as the app now routes directly to the login page.
  return null;
};

export default LandingPage;
