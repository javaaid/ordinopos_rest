import React from 'react';

const FloorPlanView: React.FC = () => {
  // This component is a placeholder for a file that was empty.
  // The floor plan functionality is handled by `TableServicesView.tsx`.
  return null;
};

export default FloorPlanView;