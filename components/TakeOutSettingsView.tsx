import React from 'react';

const TakeOutSettingsView: React.FC = () => {
  // This component is a placeholder for a file that was empty.
  // The functionality is handled by `TakeAwaySettingsView.tsx`.
  return null;
};

export default TakeOutSettingsView;
